# TASK 127 — one-shot JOM 7126 EITI procurement accounting-field scan

This task consumes one exact municipal-primary Jornal Oficial PDF selected by TASK 126.

If bytes arrive, the task computes SHA-256, uses native pypdf text extraction only, and scans the edition for a fixed accounting vocabulary. No OCR, discovery, second source or retry is allowed.

A direct policy marker plus a stable budget/accounting identifier can create only a CANDIDATE policy-to-budget bridge. Transaction identity still requires a later primary execution event.

R$30/hour, monthly payment language, contract templates, PME linkage, Program 2001 similarity and amount equality remain non-probative for transaction identity.
