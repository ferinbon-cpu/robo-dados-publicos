# TASK 125 — alternate one-shot TCESP ZIP transport

TASK 124 consumed one exact-source attempt and stopped with zero bytes. TASK 125 is a fresh, separately gated transport contract using curl.

The live gate permits exactly one GET of the same exact TCESP URL, no HEAD, no redirects and no retry. If bytes arrive, all parsing and matching occurs locally.

TCESP/Audesp remains SECONDARY_AGGREGATOR. Any discovered policy/accounting bridge is a candidate requiring primary municipal verification. No raw ZIP or CSV is committed or written to Drive.
