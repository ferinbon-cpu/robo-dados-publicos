# TASK 153 — corrected PNCP API open, authorization unit 2/10

TASK 152 successfully opened the bare PNCP origin and observed the official PNCP surface through its official gov.br redirect. Authorization unit 1/10 is consumed; 9 units remain.

This task uses authorization unit **2/10** for exactly one post-merge web open of the corrected procurement-publication API request:

`https://pncp.gov.br/api/consulta/v1/contratacoes/publicacao?dataInicial=20251128&dataFinal=20260904&codigoModalidadeContratacao=12&cnpj=45132495000140&pagina=1&tamanhoPagina=50`

The web layer may reorder query parameters. Such reordering is accepted only when the scheme, host, path, parameter names, parameter values and multiplicity are unchanged. No semantic parameter mutation is authorized.

There is no search, click, retry, follow-up open, direct download or raw persistence.

A positive administrative identifier candidate may be retained at most as `CORROBORATED`. This bounded page cannot establish PNCP `NO_MATCH` or an exhaustive negative conclusion. Municipal-primary verification remains mandatory, and no automatic financial, transaction or supplier identity may be created.

The single web-open invocation consumes unit 2 and leaves **8/10** units.
