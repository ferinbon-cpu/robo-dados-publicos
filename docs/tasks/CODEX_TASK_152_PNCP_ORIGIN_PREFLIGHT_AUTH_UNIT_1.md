# TASK 152 — PNCP origin preflight, authorization unit 1/10

After TASK 151, the parameterized PNCP URL was stopped by the web layer before source reach because tool-side query normalization did not satisfy the URL-safety precondition.

The owner then granted a fresh batch: `10 tokens de autorização concedidos`.

This task consumes only authorization unit **1/10** and, after merge, authorizes exactly one web open of the bare PNCP origin:

`https://pncp.gov.br/`

No parameterized API URL is authorized by this task. There is no search, click, retry, follow-up open, direct download, raw persistence, Drive effect or publication.

The purpose is transport preflight only: establish the official PNCP origin as previously viewed before a later, separately gated parameterized request.

A tool or transport failure is not a PNCP response. This task cannot create a candidate, PNCP NO_MATCH, exhaustive negative conclusion, financial identity, transaction identity or supplier linkage.

The single origin-open invocation consumes unit 1. The ledger then retains **9/10** units for separately merged future gates.
