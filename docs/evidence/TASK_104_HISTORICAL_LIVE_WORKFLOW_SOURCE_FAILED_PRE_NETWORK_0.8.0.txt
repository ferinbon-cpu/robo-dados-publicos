name: TASK 104 historical PPA primary evidence live once
run-name: "TASK 104 one bounded historical PPA acquisition by @${{ github.actor }}"

on:
  push:
    branches:
      - task-104-historical-ppa-live-once
    paths:
      - '.github/workflows/task-104-historical-ppa-live-once.yml'

permissions:
  contents: read

concurrency:
  group: task-104-historical-ppa-live-once
  cancel-in-progress: false

jobs:
  bounded-readonly-primary-ppa-acquisition:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
          check-latest: false

      - name: Verify local PDF text extractor without installing anything
        shell: bash
        run: |
          set -euo pipefail
          command -v pdftotext
          pdftotext -v 2>&1 | head -n 2

      - name: Execute exactly one bounded read-only historical PPA acquisition
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runtime
          python scripts/run_task104_historical_ppa_live_once.py

      - name: Emit sanitized result for immutable run-log readback
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          echo TASK104_RESULT_JSON_BEGIN
          cat runtime/task104_result.json
          echo TASK104_RESULT_JSON_END

      - name: Upload sanitized bounded evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: task-104-historical-ppa-primary-evidence
          path: runtime/task104_result.json
          if-no-files-found: error
          retention-days: 1
