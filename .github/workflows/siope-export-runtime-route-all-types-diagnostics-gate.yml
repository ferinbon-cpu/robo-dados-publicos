name: M7 SIOPE EXPORT RUNTIME ROUTE ALL TYPES DIAGNOSTICS GATE
run-name: "M7 SIOPE export runtime route all-types diagnostics 0.8.0 by @${{ github.actor }}"

on:
  workflow_dispatch:
    inputs:
      confirm_export_runtime_route_all_types_diagnostics:
        description: "Confirmar inventário sanitizado de todos os HTTP GET/POST pós-clique, todos abortados antes da rede"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: robo-dados-publicos-siope-export-runtime-route-all-types-diagnostics
  cancel-in-progress: false

jobs:
  diagnose:
    name: Inventariar todos os tipos de rota runtime abortados do export SIOPE
    if: ${{ inputs.confirm_export_runtime_route_all_types_diagnostics == true }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONDONTWRITEBYTECODE: "1"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
          check-latest: false

      - name: Instalar dependências gerais
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Instalar dependência isolada do CDP
        run: python -m pip install --disable-pip-version-check -r requirements-runtime-probe.txt

      - name: Preflight offline da candidata
        run: python scripts/github_preflight.py

      - name: Validar desenho fail-closed do runtime probe
        run: python scripts/github_siope_export_runtime_route_probe_design_gate.py

      - name: Dry-run do diagnóstico all-types sem navegador e sem rede
        run: python scripts/github_siope_export_runtime_route_all_types_diagnostics_gate.py --dry-run

      - name: Compilar
        run: python -m compileall -q .

      - name: Testes unitários
        run: python -m unittest discover -s tests -v

      - name: Regressão histórica
        run: python main.py selftest

      - name: Diagnóstico runtime all-types ao vivo com todas as requisições pós-clique abortadas
        id: runtime_gate
        shell: bash
        run: |
          set +e
          python scripts/github_siope_export_runtime_route_all_types_diagnostics_gate.py > "$RUNNER_TEMP/siope_export_runtime_route_all_types_result.json"
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Preparar evidência sanitizada
        if: ${{ always() }}
        shell: bash
        run: |
          mkdir -p siope-export-runtime-route-all-types-evidence
          if [ -s "$RUNNER_TEMP/siope_export_runtime_route_all_types_result.json" ]; then
            cp "$RUNNER_TEMP/siope_export_runtime_route_all_types_result.json" siope-export-runtime-route-all-types-evidence/result.json
          else
            printf '%s\n' '{"status":"STOP_M7_SIOPE_EXPORT_RUNTIME_ROUTE_ALL_TYPES_DIAGNOSTICS_NO_RESULT","candidate_route_network_sent":false,"response_body_captured":false,"request_body_captured":false,"request_headers_captured":false,"cookies_captured":false,"artifact_downloaded":false,"head_request_performed":false,"remote_writes":"NONE","collection_authorized":false,"processing_authorized":false,"recurrence_authorized":false,"schedule_enabled":false}' > siope-export-runtime-route-all-types-evidence/result.json
          fi

      - name: Publicar evidência sanitizada
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: siope-export-runtime-route-all-types-diagnostics-${{ github.run_id }}
          path: siope-export-runtime-route-all-types-evidence/result.json
          if-no-files-found: error
          retention-days: 30

      - name: Propagar resultado do gate
        if: ${{ always() }}
        shell: bash
        run: |
          cat siope-export-runtime-route-all-types-evidence/result.json
          code='${{ steps.runtime_gate.outputs.exit_code }}'
          if [ "${code:-26}" != "0" ]; then
            exit "${code:-26}"
          fi
          echo "PASS_M7_SIOPE_EXPORT_RUNTIME_ROUTE_ALL_TYPES_DIAGNOSTICS_GATE"
