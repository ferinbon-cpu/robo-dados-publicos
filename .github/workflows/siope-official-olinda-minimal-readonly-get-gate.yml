name: M7 SIOPE OFFICIAL OLINDA MINIMAL READONLY GET GATE
run-name: "M7 SIOPE official Olinda minimal readonly GET 0.8.0 by @${{ github.actor }}"

on:
  workflow_dispatch:
    inputs:
      confirm_official_olinda_minimal_readonly_get:
        description: "Confirmar exatamente um GET público, somente leitura, não-Limeira; sem redirect, paginação, persistência de linhas ou promoção automática"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  verify:
    if: ${{ inputs.confirm_official_olinda_minimal_readonly_get == true }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
          check-latest: false
      - run: python -m pip install --disable-pip-version-check -r requirements.txt
      - run: python scripts/github_preflight.py
      - name: Revisar evidência oficial e corroboração externa pinadas
        run: python scripts/github_siope_official_olinda_exact_contract_corroboration_review_gate.py
      - name: Validar design do GET mínimo sem rede
        run: python scripts/github_siope_official_olinda_minimal_readonly_get_gate.py --dry-run
      - run: python -m compileall -q .
      - run: python -m unittest discover -s tests -v
      - run: python main.py selftest
      - name: Executar um único GET mínimo SIOPE Olinda
        id: live
        continue-on-error: true
        run: python scripts/github_siope_official_olinda_minimal_readonly_get_gate.py --output siope-official-olinda-minimal-readonly-get-evidence/result.json
      - name: Publicar evidência sanitizada
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: siope-official-olinda-minimal-readonly-get-${{ github.run_id }}
          path: siope-official-olinda-minimal-readonly-get-evidence/result.json
          if-no-files-found: error
          retention-days: 30
      - name: Propagar STOP do gate
        if: ${{ steps.live.outcome == 'failure' }}
        run: exit 1
