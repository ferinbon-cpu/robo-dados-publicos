name: TASK 247 JOM rolling bounded discovery
run-name: "TASK 247 JOM rolling discovery by @${{ github.actor }}"

on:
  push:
    branches:
      - task-247-jom-rolling-discovery-runtime
    paths:
      - 'runtime_triggers/task247_jom_rolling_discovery.run'

permissions:
  contents: read

concurrency:
  group: task-247-jom-rolling-discovery
  cancel-in-progress: false

jobs:
  discover-delta:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout exact runtime head
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Validate authorization-only runtime delta
        shell: bash
        run: |
          set -euo pipefail
          AUTH=runtime/task247_owner_authorization.json
          test -f "$AUTH"
          IMPL="$(python - <<'PY'
          import json
          print(json.load(open("runtime/task247_owner_authorization.json", encoding="utf-8"))["implementation_sha"])
          PY
          )"
          test "$(printf '%s' "$IMPL" | grep -E '^[0-9a-f]{40}$')" = "$IMPL"
          git cat-file -e "$IMPL^{commit}"
          git merge-base --is-ancestor "$IMPL" HEAD
          UNEXPECTED="$(git diff --name-only "$IMPL"..HEAD | grep -Ev '^(runtime/task247_owner_authorization.json|runtime_triggers/task247_jom_rolling_discovery.run)$' || true)"
          test -z "$UNEXPECTED"
          echo "TASK247_IMPLEMENTATION_SHA=$IMPL" >> "$GITHUB_ENV"

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        shell: bash
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Execute exact authorized rolling discovery
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p runtime_out
          python - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path

          from robo_dados_publicos.research.task247_jom_rolling_discovery import (
              LiveJournalDiscoverySource,
              execute,
              load_config,
          )

          cfg = load_config()
          auth = json.loads(Path("runtime/task247_owner_authorization.json").read_text(encoding="utf-8"))
          result = execute(
              cfg,
              source=LiveJournalDiscoverySource(),
              authorization=auth,
              expected_implementation_sha=os.environ["TASK247_IMPLEMENTATION_SHA"],
              offline_test_mode=False,
          )
          canonical = json.dumps(result, ensure_ascii=False, sort_keys=True, separators=(",", ":")).encode("utf-8")
          result["result_sha256"] = hashlib.sha256(canonical).hexdigest()
          target = Path("runtime_out/task247_sanitized_discovery_result.json")
          target.write_text(json.dumps(result, ensure_ascii=False, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          print("TASK247_STATUS=" + str(result.get("status")))
          print("TASK247_DELTA_COUNT=" + str(result.get("delta_count")))
          print("TASK247_INDEX_PAGES=" + str(result.get("index_pages_fetched")))
          print("TASK247_EST_REMOTE_GETS=" + str(result.get("estimated_remote_get_count")))
          print("TASK247_DOCUMENT_DOWNLOADS=" + str(result.get("document_download_count")))
          print("TASK247_DRIVE_WRITES=" + str(result.get("drive_write_count")))
          if result.get("status") != "PASS_JOM_ROLLING_DELTA_DISCOVERY":
              raise SystemExit(2)
          PY

      - name: Verify sanitized-only persistence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          test -f runtime_out/task247_sanitized_discovery_result.json
          test ! -e runtime_out/raw.html
          test ! -e runtime_out/source.pdf

      - name: Upload sanitized rolling discovery result only
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: task-247-jom-rolling-sanitized-discovery
          path: runtime_out/task247_sanitized_discovery_result.json
          if-no-files-found: error
          retention-days: 1
