name: M7 SIOPE EXPORT RUNTIME ROUTE DIAGNOSTICS GATE
run-name: "M7 SIOPE export runtime route diagnostics 0.8.0 by @${{ github.actor }}"

on:
  workflow_dispatch:
    inputs:
      confirm_export_runtime_route_diagnostics:
        description: "Confirmar diagnóstico runtime com inventário sanitizado e abort antes da rede"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: robo-dados-publicos-siope-export-runtime-route-diagnostics
  cancel-in-progress: false

jobs:
  diagnostics:
    name: Inventariar rotas runtime abortadas do export SIOPE
    if: ${{ inputs.confirm_export_runtime_route_diagnostics == true }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONDONTWRITEBYTECODE: "1"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
          check-latest: false

      - name: Instalar dependências gerais
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Instalar dependência isolada do CDP
        run: python -m pip install --disable-pip-version-check -r requirements-runtime-probe.txt

      - name: Preflight offline da candidata
        run: python scripts/github_preflight.py

      - name: Validar desenho fail-closed do runtime probe
        run: python scripts/github_siope_export_runtime_route_probe_design_gate.py

      - name: Dry-run do diagnóstico sem navegador e sem rede
        run: python scripts/github_siope_export_runtime_route_diagnostics_gate.py --dry-run

      - name: Compilar
        run: python -m compileall -q .

      - name: Testes unitários
        run: python -m unittest discover -s tests -v

      - name: Regressão histórica
        run: python main.py selftest

      - name: Diagnóstico runtime ao vivo com todas as requisições pós-clique abortadas
        id: diagnostics_gate
        shell: bash
        run: |
          set +e
          python scripts/github_siope_export_runtime_route_diagnostics_gate.py > "$RUNNER_TEMP/siope_export_runtime_route_diagnostics_result.json"
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Preparar evidência sanitizada
        if: ${{ always() }}
        shell: bash
        run: |
          mkdir -p siope-export-runtime-route-diagnostics-evidence
          if [ -s "$RUNNER_TEMP/siope_export_runtime_route_diagnostics_result.json" ]; then
            cp "$RUNNER_TEMP/siope_export_runtime_route_diagnostics_result.json" siope-export-runtime-route-diagnostics-evidence/result.json
          else
            printf '%s\n' '{"status":"STOP_M7_SIOPE_EXPORT_RUNTIME_ROUTE_DIAGNOSTICS_NO_RESULT","candidate_route_network_sent":false,"response_body_captured":false,"request_body_captured":false,"request_headers_captured":false,"cookies_captured":false,"artifact_downloaded":false,"head_request_performed":false,"remote_writes":"NONE","collection_authorized":false,"processing_authorized":false,"recurrence_authorized":false,"schedule_enabled":false}' > siope-export-runtime-route-diagnostics-evidence/result.json
          fi

      - name: Publicar evidência sanitizada
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: siope-export-runtime-route-diagnostics-${{ github.run_id }}
          path: siope-export-runtime-route-diagnostics-evidence/result.json
          if-no-files-found: error
          retention-days: 30

      - name: Propagar resultado do gate
        if: ${{ always() }}
        shell: bash
        run: |
          cat siope-export-runtime-route-diagnostics-evidence/result.json
          code='${{ steps.diagnostics_gate.outputs.exit_code }}'
          if [ "${code:-25}" != "0" ]; then
            exit "${code:-25}"
          fi
          echo "PASS_M7_SIOPE_EXPORT_RUNTIME_ROUTE_DIAGNOSTICS_GATE"
